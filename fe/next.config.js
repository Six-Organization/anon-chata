/** @type {import('next').NextConfig} */
const nextConfig = {
  // Output standalone untuk image Docker yang ramping.
  output: "standalone",
  reactStrictMode: true,
};

module.exports = nextConfig;
